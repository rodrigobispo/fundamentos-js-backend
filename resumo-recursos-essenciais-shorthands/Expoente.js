function power(base, exponent) {
  return Math.pow(base, exponent);
}
console.log(power(2, 3));  // Output: 8


const base = 3;
const exponent = 3;
console.log(base ** exponent);  // result is now 27
